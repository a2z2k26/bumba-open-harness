# {{sectionName}} Specification

## User Flows

{{userFlows}}

## UI Requirements

{{uiRequirements}}

## Data Requirements

{{#if dataRequirements}}
{{dataRequirements}}
{{else}}
See `types.ts` for TypeScript interfaces and `data.json` for sample data structure.
{{/if}}

## Component References

{{#if bumbaComponentsAvailable}}
### Available Bumba Components

The following components are available in `.design/components/`:

{{#each bumbaComponents}}
- **{{this.name}}** ({{this.type}})
{{/each}}

**Implementation**: Use these components where applicable. Components are available in `.design/extracted-code/{{framework}}/` after transformation.

If targeting a different framework, run `/design-transform-[framework]` to convert components.
{{else}}
### Component Requirements

Components should be designed and extracted using Bumba Design features:

1. Design components in Figma following the design system
2. Use Bumba Figma plugin to extract component definitions
3. Transform to {{framework}} using `/design-transform-{{framework}}`

The implementation should reference Bumba's component library for consistent patterns.
{{/if}}

## Screen Specifications

{{#if screens}}
{{#each screens}}
### {{this.name}}

**Purpose**: {{this.purpose}}

**Data Displayed**: {{this.dataDisplayed}}

**Actions Available**: {{this.actions}}

**States**: {{this.states}}

---

{{/each}}
{{else}}
(Use `/director-screen-spec` to add screen specifications)
{{/if}}

## Testing Requirements

- Test all user flows end-to-end
- Verify data loading and error states
- Test responsive behavior across breakpoints
- Verify accessibility (keyboard navigation, screen readers)
- Test empty states and edge cases

---

*Generated by Bumba Design Director*
