# Data Model

This document defines the core entities and relationships in the system.

## Entities

{{#each entities}}
### {{this.name}}

{{this.description}}

**Attributes**:
{{#each this.attributes}}
- `{{this.name}}`: {{this.type}}{{#if this.description}} - {{this.description}}{{/if}}
{{/each}}

{{#if this.relationships}}
**Relationships**:
{{#each this.relationships}}
- {{this.description}}
{{/each}}
{{/if}}

---

{{/each}}

## Notes

- This data model defines structure only, not design aesthetics
- TypeScript interfaces are available in `types.ts`
- Actual implementation may include additional fields (timestamps, IDs, etc.)
- Relationships should be implemented according to your chosen data layer

---

*Generated by Bumba Design Director*
