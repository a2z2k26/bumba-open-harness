"""Bridge operator scripts (Sprint 3.01 of #1112 master plan).

Lightweight read-only scripts that consume runtime artefacts (service state
files, registries, plists) and produce operator-readable reports. These do
NOT modify runtime state — they are diagnostic by design.
"""
